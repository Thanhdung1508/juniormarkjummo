# Copy file này thành .env.local, điền giá trị từ Supabase → Project Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
# Chấp nhận publishable key (sb_publishable_...) hoặc legacy anon key.
# Tuyệt đối không dùng service_role / secret key trong biến VITE_*.
